{
  "name": "personal-site",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "deploy": "stylus -c styles.styl && postcss -u autoprefixer cssnano -c ~/.autoprefixer.json styles.css -o styles.min.css && now",
    "start": "serve"
  },
  "devDependencies": {
    "stylint": "^1.5.9",
    "stylus": "^0.54.5"
  },
  "dependencies": {
    "serve": "^4.0.1"
  }
}
